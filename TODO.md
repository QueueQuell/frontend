# Image Upload Implementation for Item Create/Update

## Overview

Replace image URL input with file upload in ItemForm. Update create/edit pages and services to use FormData with 'images' field for backend Cloudinary upload.

## Steps to Complete

### 1. [ ] Update src/lib/api/client.ts

- Add `postFormData(url, formData)` and `putFormData(url, formData)` methods
- Use fetch with FormData (no Content-Type header), include Authorization
- Handle 401 refresh similar to existing methods

### 2. [ ] Update src/lib/api/services/menu.service.ts

- Add `createWithFiles(formData: FormData)` → apiClient.postFormData(CREATE, formData)
- Add `updateWithFiles(id: string, formData: FormData)` → apiClient.putFormData(UPDATE(id), formData)

### 3. [ ] Update src/components/items/ItemForm.tsx

- Replace Images TextField with `<input type="file" multiple accept="image/*" />`
- Add image preview (thumbnails)
- Add validation: max 5 files, 5MB each
- Change props: accept `files: File[]`, `onFilesChange: (files: File[]) => void`, `existingImages?: ImageDto[]`
- Remove old images array management

### 4. [ ] Update src/app/(dashboard)/catalog/items/create/page.tsx

- Add `files: File[] = []` state
- Pass to ItemForm: files, onFilesChange
- handleSubmit: Build FormData → append fields → append('images', file) for each → menuService.createWithFiles(formData)

### 5. [ ] Update src/app/(dashboard)/catalog/items/[id]/edit/page.tsx

- Load `existingImages: data.imageUrls || []`
- Add `files: File[] = []` state
- Pass to ItemForm with existing
- handleSubmit: FormData same as create → menuService.updateWithFiles(id, formData)

### 6. [ ] Test

- `npm run dev`
- Create item with images → check backend upload, list shows images
- Edit item → add/replace images
- Validate max files/size

## Progress

✅ Step 1: src/lib/api/client.ts - FormData methods added
✅ Step 2: src/lib/api/services/menu.service.ts - createWithFiles/updateWithFiles added

✅ Step 3: src/components/items/ItemForm.tsx - File upload UI with preview and validation added

✅ Step 5: src/app/(dashboard)/catalog/items/[id]/edit/page.tsx - File upload support added

## Progress

✅ All code changes complete!

### 6. [ ] Test

- `npm run dev`
- Create item with images → check backend upload, list shows images
- Edit item → add/replace images
- Validate max files/size

---

_Generated by BLACKBOXAI_

---

_Generated by BLACKBOXAI_
